# Pipeline — Pending Job URLs (Intl track)

<!-- This file holds the **intl (non-home-country) track**. The sibling
     `data/pipeline.md` holds the home-country track. Same format, same
     rules, same evaluation flow — just different locations.

     Both tracks dedup against each other plus against
     `data/applications.md` (the unified application tracker).

     Copy this file to `data/intl/pipeline.md` to start using the intl
     track, OR just run `node scan.mjs --intl` and the scanner will
     create it for you. -->

## Pendientes

<!-- ▼ Manual additions — paste new URLs below as: `- [ ] {url} | {Company} | {Role} | {Location}` -->



## Procesadas
